```php
shuffle($배열) --> 임의로 섞기

foreach($배열 as $변수)  --> 배열의 값을 변수에 넣어서 반복
ex) foreach($arr as $data)

sort($배열) --> 오름차순으로 정렬

rsort($배열) --> 내림차순으로 정렬

$newArr = array_reverse($배열) --> 역순으로 정렬
// 배열 자체를 변경하지 않고, 새로운 배열 반환.
```
